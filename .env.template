# ==================================================
# CONFIGURACIÓN DE ENTORNO - DASHBOARD OBEYA 2026
# ==================================================
# Copiar este archivo como .env y configurar las variables según el entorno

# ----- DESARROLLO LOCAL -----
# Ruta a la base de datos SQLite (desarrollo local)
DATABASE_PATH_LOCAL=C:\Users\williamcc\Desktop\Developer\DataBase_SQLite\Maestro.db

# Ruta a archivos geográficos (desarrollo local)
GEODATA_PATH_LOCAL=C:\Users\williamcc\Desktop\Developer\GeoData

# ----- PRODUCCIÓN (STREAMLIT CLOUD / SERVER) -----
# Ruta a la base de datos SQLite (producción)
DATABASE_PATH=/app/data/Maestro.db

# Ruta a archivos geográficos (producción)
GEODATA_PATH=/app/geodata

# ----- CONFIGURACIÓN DE LA APLICACIÓN -----
# Puerto (si se ejecuta en servidor propio)
PORT=8501

# Modo debug (True/False)
DEBUG=False

# Caché TTL (segundos) - tiempo de vida del caché de datos
CACHE_TTL=300

# ----- CONFIGURACIÓN DE STREAMLIT -----
# Estas variables se pueden configurar en .streamlit/config.toml
# o como variables de entorno con el prefijo STREAMLIT_

# Tema de colores
STREAMLIT_THEME_PRIMARY_COLOR=#1e3c72
STREAMLIT_THEME_BACKGROUND_COLOR=#ffffff
STREAMLIT_THEME_SECONDARY_BACKGROUND_COLOR=#f0f2f6
STREAMLIT_THEME_TEXT_COLOR=#262730

# Configuración del servidor
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_FILE_WATCHER_TYPE=none

# Configuración de navegador
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
STREAMLIT_BROWSER_SERVER_ADDRESS=localhost
STREAMLIT_BROWSER_SERVER_PORT=8501

# ----- NOTAS -----
# 1. Para Streamlit Cloud, configurar los secrets en el dashboard de Streamlit
# 2. Para deploy en servidor propio, usar este archivo .env
# 3. Nunca commitear este archivo con valores reales a git
# 4. Los archivos geográficos (.shp, .geojson) deben estar en la carpeta especificada
